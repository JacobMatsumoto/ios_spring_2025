/*I'll need variables to create the fib seq,
+= to do the adding
an array to hold it
a function to house it
a while statement to make it continue to do its thing.
.append to add the proper numbers to the array

Set up the function.
set up variables set to 0 and 1 
make an empty Int array  
figure out the math required to make the fib seq.
append the appropriate numbers
print the list.
call the function
